# End-DPScope 功能说明

> 明日方舟：终末地 · 排轴伤害计算器
> 版本 1.0（x64）· Windows 10 / 11

---

## 一、这是什么

把你在 Endaxis 里排好的技能轴（导出的 JSON 文件）导入进来，程序会**逐段重算每一击的伤害**，
然后生成一份 Excel 报告，告诉你：

- 每一次命中的**非暴击伤害 / 暴击伤害 / 期望伤害**（可以直接和游戏实战对账）
- 整段轴的总伤害、DPS、战斗时长
- 每个干员的伤害占比、拐力占比（LMDI 归因）、实际贡献占比
- 每一击的**完整乘区拆解**：基础伤害区、暴击区、加成区、增幅区、脆弱区、易伤区、
  防御区、抗性区、失衡区、抗性削减……以及**每个乘区的来源**（哪件装备、哪个 Buff 给的）
- 每一击**命中瞬间敌人的实时状态**（元素附着层数、身上挂着的 debuff、是否处于失衡）

计算核心是开源的 **Endaxis 模拟器**（`github.com/Lieyuan621/Endaxis`），
本项目在它之上补齐了逐段明细导出、乘区溯源、拐力归因明细、
敌人实时状态快照、Excel 报告与图形界面。

---

## 二、安装与启动（三步）

**好消息：不需要安装 Python，也不需要安装 Node.js，全部已内置。**

| 步骤 | 操作 |
|---|---|
| 1 | 把 `End-DPScope-v1.0-x64.zip` 解压到任意目录（例如 `D:\End-DPScope`） |
| 2 | 双击 `End-DPScope.exe` |
| 3 | 在窗口里点「浏览…」选排轴 JSON → 选敌人 → 点「开始计算并生成 Excel 报告」 |

> **注意：请勿只把 `End-DPScope.exe` 单独复制出来使用。**
> 它需要同目录下的 `vendor\`（计算核心）与 `runtime\`（Node 运行时）。
> 想换位置就整个文件夹一起移动。

启动后主题（白天/黑夜）会被记住，下次打开沿用上次的选择。

---

## 三、界面说明

界面是**双栏工作台**：左侧从上到下是配置流程——① 排轴文件 → ② 敌人配置 →
③ 队伍配置（选了文件才出现）→ ④ 面板修正（可选），填完点最下面的按钮开始计算；
右侧是**观测读数面板**（桌面窗口常驻显示，计算前是操作引导，计算完成后展示
DPS 大读数、总伤害/DPSP/敌人剩余血量、贡献率、每秒输出曲线与报告操作按钮）。
窗口较窄或网页端在手机上时，读数面板会自动挪到「开始计算」按钮下方。

顶栏右侧从左到右：游戏数据同步状态（点击立即同步）· 皮肤切换（结束剂/罗德岛/拓荒者）·
明暗主题切换 · 功能说明。首次启动默认暗色「结束剂」皮肤，选择会被记住。

### ① 排轴文件

点「浏览…」选择 JSON 文件，支持两种来源：

- **Endaxis 导出的排轴**：在 Endaxis 网页/程序里使用「导出」功能得到的 JSON。
- **本项目原生配置**：由本项目的配置文件直接描述干员、武器、装备与技能顺序
  （参考 `examples\样例配置.json`）。

选定文件后，程序会自动读取里面的敌人配置并填到下面第②块。

### ② 敌人配置

三个预设，选一个：

| 预设 | 含义 | 什么时候用 |
|---|---|---|
| **排轴内敌人** | 完全按 JSON 文件里的敌人设置 | 想复现排轴作者的条件 |
| **训练木桩** | 0 元素抗性、血量极大、不会被打死 | 测「纯输出上限」，比较不同轴的强弱 |
| **自定义** | 手动填面板 | 想算「能不能打死某个具体 Boss」 |

选「自定义」后可以填：

| 字段 | 说明 |
|---|---|
| 血量 | 敌人总血量。例：天鼓（战争回响）= `3962145.6` |
| 防御 | 敌人防御力，默认 100。**注意：排轴里带了敌人 ID 时，防御以该敌人模板为准** |
| 元素抗性 | 物理 / 灼热 / 寒冷 / 电磁 / 自然，五个独立填写（单位 %） |
| 失衡上限 | 打满多少失衡值敌人进入失衡（处决）状态 |
| 失衡时长 | 失衡状态持续多少秒 |
| 处决回技 | 处决命中后回复多少技力 |

### 「敌人模板」预设：直接用真实数值

不用手抄。选「敌人模板」，填模板 ID（选排轴文件后会自动预填）、等级和血量倍率，
程序直接读 AKEData 解包数据。例如天鼓：

- 模板 ID `eny_0082_hsbear`、等级 90、血量倍率 `1.0` → 真实模板数值（全抗 0、HP 1,100,596）
- 血量倍率 `3.6` → 战争回响难度的 3,962,145.6

**排轴内 ≠ 真实模板**：排轴作者可以在排轴里自定义敌人抗性/血量。
选排轴文件后，界面会显示「排轴内实际值」摘要，方便对照。
计算完成后，结果卡片和 Excel「总览」都会写明**本次实际使用的敌人配置**
（HP / 全抗 / 防御 / 失衡上限 / 失衡时长），任何对不上都可以直接核对。

### ③ 队伍配置（载入排轴后出现）

载入排轴文件后，这里会按轴里的四个人列出**这名干员在这条轴里的实际配置**，
每一项都可以直接改，改完点的还是同一个「开始计算并生成 Excel 报告」：

| 可改的项 | 影响 |
|---|---|
| 等级 / 精英 / 潜能 / 信赖 | 面板攻击、防御、生命 → 攻击区、基础伤害 |
| **技能等级（含专精）** | 普攻 / 战技 / 连携 / 终结技各自的倍率，按 1~12 级重算每一段 |
| 天赋Ⅰ / 天赋Ⅱ | 天赋等级对应的触发器效果 |
| **武器**（下拉） | 换成**同类型**武器，武器等级 / **突破** / 潜能一起改 |
| **装备**（下拉 ×4） | 每个部位换成同部位部件；每条词条一个**精锻**等级（0-3），旁边显示当前数值 |

要点：

- 改动**只影响本次计算**，不会写回排轴文件。想恢复原样，重新选一次排轴文件即可。
- 「武器技能」一行显示的是**这把武器自己的技能名**（如 赫拉芬格的 力量 / 攻击力 / 迸发·切骨之寒），
  悬停可看技能描述与当前等级下的合法范围；装备词条同样悬停可看各级精锻数值。
- 装备卡底部会显示「已穿套装」徽章（如 `清波 3/3 ✓`）。**套装是三件才起效果**，
  换掉一件掉到 2/3 时徽章会变灰，提示你这套效果已经没了。
- 武器/装备/练度改变会连带改变**套装效果、触发器与 CD**，这些都会跟着重算。
- 程序**不校验轴的技力/充能/CD 可行性**：轴上动作按时刻表强制结算，
  配置改到轴上原本打不出来也照算（即「非法轴口径」）。这样换配置时
  伤害口径保持可比，不会因为程序擅自删掉动作而失真。

### 皮肤与主题

界面右上角有三个圆形色点，可切换三套完整美术方案，每套都有白天/黑夜两态：

| 皮肤 | 风格 |
|---|---|
| **结束剂**（默认） | 终末地工业黑黄：警示条纹、切角按钮、墨黑与结束剂黄 |
| **罗德岛** | 冰蓝科技：深海军蓝底、冰蓝高亮 |
| **拓荒者** | 荒原暖琥珀：暖纸色底、琥珀橙点缀 |

选择会被记住。旁边的小太阳/月亮按钮切换白天与黑夜。

### 数据同步与更新（全自动）

- **游戏数据同步**：启动后程序在后台自动刷新干员中文名、敌人属性表与
  **装备/武器的官方中文名**（名称每日、敌人表与官方名每周，联网失败则静默跳过，
  不影响离线使用）。页脚显示「游戏数据同步：x 分钟前」。
- **计算核心更新检测**：页脚自动比对 Endaxis 上游；有新版本时会显示
  「计算核心有新版本」，点击可查看更新内容。新版本收录新干员后，
  下载新版安装包即可使用。

### ④ 面板修正（可选）

**绝大多数情况留空即可。** 详见下一节。

---

## 四、「面板修正」到底是什么

### 先说清楚：哪些机制不需要你填

引擎对**条件型**暴击机制是按游戏机制逐段结算的，跟着每一击命中瞬间的敌人状态走，
不需要、也不应该用一个手动数值去顶替。以伊冯为例（已在引擎内实测验证）：

| 机制 | 引擎行为 |
|---|---|
| 天赋「冰点」：对寒冷附着敌人暴伤 +20%，冻结加倍 | 命中瞬间敌人有寒冷附着 → 该击暴伤 +20%；处于冻结 → 两条同时生效（+40%） |
| 终结技「冷冻射手」：强化普攻每次攻击叠暴击率 | 每击暴击率 +3%，叠满 10 层（+30%） |
| 叠至最大层数后暴击伤害增加 | 满 10 层时暴伤 +60% |
| 结束前重击；敌人处于冻结则追加一段寒冷伤害并消耗冻结 | 重击命中时若敌人处于冻结则追加一段并消耗冻结，否则不触发 |

这类「对某状态的敌人 +X%」「叠层」「消耗状态」的机制，在生成的 Excel 里
可以直接看到：同一干员的「暴击(率/伤)」列会随命中瞬间的敌人状态逐行变化。

武器与装备同理：引擎内已建模的暴击类词条/特效（如 Certain 武器的暴击叠层、
装备套装的暴击效果）都会按条件结算。

### 那这一栏还剩什么用

只用来补**恒定的、无条件的**面板缺口——比如某件装备的固定暴击率词条
引擎没有计入，而它不加条件、永远生效。这种才能安全地用一个常数顶上。

程序内部目前为每个干员算出的基础面板是 **暴击率 5% / 暴击伤害 50%**。
也就是说：如果游戏里别礼面板显示 62.5% 暴击率，且你确认没有任何引擎外
的来源，那么缺口是 57.5 个百分点。

### 怎么填

填**差额（增量）**，单位是百分点：

- `all=57.5` → 全队每个人都 +57.5% 暴击率
- `last-rite=57.5` → 只给别礼（`last-rite`）加
- 多个角色用逗号隔开：`last-rite=57.5,tangtang=30`
- 两个框分别对应「暴击率」和「暴击伤害」，留空表示不修正

> **关键：填的是「游戏面板 − 模拟器算出的值」，不是游戏面板本身。**
> 填面板原值会把同一份暴击来源算两遍，结果偏高。
> **不要**把条件型机制（天赋/武器特效）折成常数填进来——那会让它们
> 在不满足条件的命中上也生效，结果失真。

### 怎么知道模拟器算的是多少

打开生成的 Excel，看**「暴击(率/伤)」**一列，那就是模拟器当前算出的面板值。
用它和游戏内面板一对减，就是要填的增量。

### 参考量级（本测试轴）

| 修正内容 | 总伤害变化 |
|---|---|
| 不修正 | 3,700,482 |
| 全队暴击率 +10 | 3,880,996 |
| 全队暴击伤害 +20 | 3,736,585 |

填完点开始计算即可。如果写了不存在的角色 ID，程序会在底部提示你哪些被忽略了。

---

## 五、报告里有什么

生成的 Excel（`原文件名_DPS报告.xlsx`，和排轴文件放在同一目录）包含多张表：

| 工作表 | 内容 |
|---|---|
| **计算过程** | 主表。顶部是总结数据，之后**每一行 = 一次命中**，按时间顺序排列。伤害三列依次为**期望 / 非暴击 / 暴击**，随后是**实时暴击率与实时暴击伤害**（伊冯、洛茜等叠暴角色逐 hit 变化），再往右是各乘区数值**及其来源** |
| **总览** | 总伤害、DPS、战斗时长、敌人剩余血量、干员贡献（LMDI）等汇总 |
| **拐力归因** | LMDI 专表：干员贡献详解、拐力去向矩阵、单击拐力 TOP15（见下文） |
| **伤害时间轴** | 每秒输出（数据条）、**失衡窗口利用**（窗口内伤害占比）、爆发 TOP5 秒 |
| **技能与元素分布** | 技能类型 / 元素 / **干员×具体动作** 三个维度的伤害分布 |
| **SP与能量** | **SP 池曲线（引擎官方投影，含自然恢复 8/s、触底与回满时刻）**、事件流水 |
| **增益时间线** | 每个 Buff 什么时候生效、什么时候结束 |
| **增益覆盖统计** | 每个增益的**时间覆盖率**与**伤害加权覆盖率**（对象在增益下打出了自己几成的伤害） |

主表怎么读：

- **时间(s)**：这一击发生的时刻
- **干员 / 动作 / 类型**：谁、用什么技能。多段技能会写成
  「汤汤战技hit1」「汤汤战技hit2」，**不会**让人误以为放了两次技能
- **敌人附着 / 敌人debuff / 失衡**：**这一击命中瞬间**敌人的实时状态
- **期望 / 非暴击 / 暴击伤害**：三列并排，方便和游戏实战对照；**实时暴击率 / 实时暴击伤害**两列显示该击命中瞬间的暴击面板（叠暴角色的暴击率会逐段上涨）
- **有效攻击力 / 倍率 / 基础伤害**：伤害的起点
- 之后的每一组「数值 + 来源」列，就是这一击的乘区计算过程

### 报告怎么打开

算完在结果卡片下面有三个按钮：

| 按钮 | 作用 |
|---|---|
| **另存为…**（桌面窗口） | 弹系统保存框，把报告另存到你选的位置 |
| **下载报告**（网页版） | 浏览器下载，等同于普通文件下载 |
| **打开文件** | 直接用 Excel 打开这份报告 |
| **定位文件** | 在资源管理器里选中报告文件，方便复制/发给别人 |

报告本身**已经自动写进程序目录下的 `output\` 文件夹**，
即使不点任何按钮，去那里也能拿到。

### 「拐力归因」表怎么读（LMDI）

伤害占比回答"谁打了多少"，拐力归因回答"谁让谁多打了多少"。辅助干员自己打出的
伤害往往很少，但她提供的增益让全队多打了上百万——这部分只有 LMDI 归因能看到。

三个指标：**自身结算**（该干员的命中去掉所有队友增益后的无拐基线）、
**拐力**（其增益给其他干员带来的提升）、**实际贡献**（两者之和）。
以本测试轴为例：赛希自身结算仅 6,463，拐力却高达 1,235,074（占总伤 33%）——
只看伤害占比会完全低估她。LMDI 分解有硬保证：全队「自身结算 + 拐力」之和
恰等于总伤害，且逐 hit 成立（191 段命中校验偏差 0），不是按比例摊派的估计。

该表分三部分：

1. **干员贡献详解**：自身结算 / 无拐占比 / 受拐（其命中里有多少是队友拐的）/
   拐力 / 拐力占比 / 实际贡献。
2. **拐力去向矩阵**：行是增益来源干员，列是受益干员（本轴：赛希→别礼 746,814、
   诀→汤汤 663,542）。
3. **单击拐力 TOP15**：最大的单次增益落在哪一击上（本轴最大一笔：赛希拐别礼连携技
   194,494，占该击 66%）——优化轴时让大倍率命中吃满增益。

另外，「计算过程」主表每行末尾也有 **LMDI拐力归因** 列（如 `自身 34%；赛希 194,494（66%）`），
程序算完还会在界面上给出拐力榜前三名。

### 拐力归因是怎么算的

这套归因**不是按比例摊派的估计**，而是对每一击做精确分解，过程完全可复核
（Excel「拐力归因」表末尾也附有同样的说明，方便转发讨论时直接引用）：

1. **构造无拐基线**：对每一击，保留命中者自身的增益和基础面板，剔除全部队友提供的
   增益 / 减益 / 连击 / 失衡贡献，用同一套伤害公式重算一次，得到"无拐基线伤害" D_self；
   该击实际的期望伤害记为 D_actual。
2. **逐乘区分解（LMDI）**：期望伤害 = 基础 × (1+加成) × (1+增幅) × 暴击期望 × (1+易伤)
   × (1+承伤) × 抗性 × 连击 × 失衡 × …。对每个乘区因子 k，
   其贡献 = L × ln(f_actual / f_self)，其中 L 是对数平均数 L(a,b) = (a−b)/ln(a/b)。
   这是乘法分解的标准 LMDI 方法（对数平均迪氏指数），乘区之间的交互影响按对数对称分摊。
3. **来源分摊**：同一乘区内有多个来源时（易伤 +20% = 甲 +15% + 乙 +5%），
   按各自加成数值的占比分摊（75% : 25%）。
4. **自身取余项**：自身 = D_actual − Σ 外部贡献。所以"自身 + Σ拐力 = 该击期望"
   **逐 hit 精确成立**，全队合计恰等于总伤害。

**口径与边界**（引用报告数据讨论时请一并带上）：

- 记账采用"**施加者**"模式：敌人身上的易伤/承伤/抗性削减，整笔记给施加该减益的干员；
  连击层数与失衡值按各干员的贡献占比拆分。
- 分解对象是**期望伤害**（暴击期望 = 1 + 暴击率 × 暴击伤害），不含单次暴击掷点的运气成分。
- 防御区、抗性区基线不受干员增益影响，不产生拐力；反应伤害（法术爆发/燃烧/冻结等）
  按消耗的附着层数单独记功。
- 基线是"该击剔除外部增益后重算"，**不是**"把该干员移出队伍重打一遍"——出招序列由
  排轴给定，队友影响出招本身的部分不在归因范围内。
- 归因方法存在其他合理选择（逐个剔除法、Shapley 值等），数值会略有差异；本报告固定使用
  LMDI，以保证全队精确可加、逐 hit 可核对。讨论"谁贡献大"时请以同口径数据为准。

---

## 六、常见问题

**Q：需要联网吗？**
不需要。计算、数据、运行时全部在本地。只有「同步 Endaxis」这一个功能需要联网（见下）。

**Q：为什么 XX 干员的伤害看着不对？**
先确认装备/武器/技能等级是否和实战一致，再确认轴的顺序和实战一致
（比如伊冯的强化普攻依赖终结技施放，轴里没放终结技就不会有强化段）。
装备/武器/技能等级都可以直接在界面的**「③ 队伍配置」**里改，
不用去动排轴文件；改完重算即可。

**Q：换了装备，套装效果会跟着变吗？**
会。套装效果是每次计算按实际穿戴**现算**的，**三件才生效**。
界面上每张干员卡的「已穿套装」徽章会实时显示 `套装名 件数/3`，
掉到 2/3 会变灰，提醒你这套效果已经没了。

**Q：把配置改强了，轴上的技能放不出来（技力/充能/连携CD 不够）会怎样？**
按**非法轴口径**计算：轴上动作按时刻表强制结算，程序不会替你删动作或判不合法。
这样换配置前后的伤害口径保持一致，便于横向比较。
反过来，如果你要的是"这套配置能不能真的打出来"，那属于排轴本身的问题，
需要在 Endaxis 里调轴。

**Q：算出来打不死敌人，游戏里却打死了？**
先检查上面两项；确认是恒定面板缺口后再用「面板修正」补差额。

**Q：自己填的敌人数值和「排轴内敌人」算出来差很多？**
先看结果卡片和 Excel「总览」里的**「本次敌人配置」**——那里写的是本次计算真正使用的
数值。最常见的两个原因：
1. 旧版程序的「面板校准」栏预填过测试值（last-rite=62.5 等），会悄悄放大伤害。
   新版已改为留空，旧包请更新。
2. 排轴内的抗性/血量是排轴作者设的，可能和真实模板不同（如战争回响天鼓为
   全抗 50、血量×3.6，而真实模板全抗 0）。50 全抗与 0 抗性的差距约 ±60%。
另外，失衡上限与失衡时长也会明显影响结果（敌人中途失衡与否能差 15-20%）。

**Q：DPS 是怎么算的？**
`总伤害 ÷ （最后一次造成伤害的时刻 − 第一次造成伤害的时刻）`。
即只算真正在输出的那段时间，不会因为轴后面留了空档而把 DPS 拉低。

**Q：能不能不用 Endaxis 导出的 JSON？**
可以。`examples\样例配置.json` 是本项目的原生配置格式，直接描述干员练度、
武器练度、装备和技能顺序，不依赖 Endaxis 的导出功能。

**Q：新干员（比如提弗洛斯）支持吗？**
提弗洛斯目前**不支持**——计算核心 Endaxis 上游还没有收录他的数据，强行从解包数据
凑倍率只会产生错误数字，所以明确报错而不是给假结果。上游收录后，维护者执行
`python sync_endaxis.py` 即可跟进（需要开发者环境，普通用户等新版安装包即可）。

**Q：程序目录里都是什么？**

| 路径 | 作用 |
|---|---|
| `End-DPScope.exe` | 主程序（双击这个） |
| `runtime\node.exe` | 内置的 Node 运行时（请勿删除） |
| `vendor\endaxis\` | 计算核心（Endaxis 模拟器 + 本项目的数据导出桥） |
| `dps_end\data\names.json` | 干员/敌人中文名映射 |
| `.akedata_cache\` | 敌人属性数据缓存 |
| `assets\` | 图标 |
| `环境自检与修复.bat` | 启动异常时双击它自检 |

---

## 七、出问题了怎么办

1. **双击 `环境自检与修复.bat`**，它会检查 `runtime\node.exe`、`vendor\endaxis\node_modules`
   等关键文件是否齐全，缺什么会告诉你。
2. 确认解压时**没有只解压出 exe**，而是整个文件夹。
3. 确认路径里**没有特殊字符**（建议放在 `D:\End-DPScope` 这类简单路径，避开中文路径）。
4. 把窗口底部的报错信息截图反馈给维护者。

---

## 八、数据与致谢

- 计算核心：[Endaxis](https://github.com/Lieyuan621/Endaxis)（开源）
- 干员 / 武器 / 装备 / 敌人数据：解包数据（AKEData / akedata.wiki）
- 敌人属性缓存：`.akedata_cache\`

本项目仅用于伤害计算与研究，游戏内实际数值以官方为准。
