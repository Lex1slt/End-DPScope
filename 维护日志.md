# DPS-END 维护日志

## 2026-09-20　前端整体重设计（End-DPScope 双栏工作台）

**范围**：仅 `web/` 前端与文档，计算/报告/同步链路零改动。

**改了什么**：
1. **布局**：单列居中 → 双栏工作台（`lg:grid-cols-[1fr_396px]`）。左侧配置流（①排轴→②敌人→③队伍→④修正→计算按钮），右侧粘性「观测读数面板」：计算前是操作引导，完成后是 DPS 大读数（40px Bahnschrift）+ 指标格 + 贡献率堆叠条（实心=自身伤害、斜纹=拐力）+ 每秒输出曲线（104px 高、峰值高亮、基准网格线）+ 报告操作。面板高于视口时内部滚动，底部按钮始终可达。窄窗（<lg）自动折为单列，结果跟在按钮后。
2. **设计系统**（index.css 重写）：新增 `--panel/--border-strong/--warn` 令牌；三皮肤 × 明暗全部重调对比度（正文/提示均 ≥4.5:1）；`.num`（Bahnschrift DIN 风格数字，Windows 内置、离线可用）、`.frame-ticks`（读数面板四角刻度框）、`.stripe-buff`（拐力斜纹）、`.chart-grid`、细滚动条、全局 focus-visible。警示条纹收敛为页头一处品牌瞬间。**默认主题改为暗色**（已存偏好不受影响）。
3. **组件**：primitives 扩展 Panel/Select/Notice/FieldCell/ToggleChip；App.tsx 从 1420 行拆为 App（壳+状态）+ CrewEditor（队伍字段格：标签在上、控件对齐，替代旧「行内挤标签」）+ ResultPane（读数面板，含 DPS/总伤数字滚动动效，尊重 prefers-reduced-motion）+ lib/crew.ts（类型与目录工具）。DPS 计算完成时数字 count-up + 条形生长是全页唯一编排动效。
4. **交互**：游戏数据同步状态从页脚挪到顶栏胶囊（点击即同步）；计算核心更新提示改为顶栏下横条（原页脚链接）；载入文件后拖放区显示「点击更换」；错误改 Notice 组件。
5. **顺带修复**（原代码有、但 vite build 不跑 tsc 一直没暴露）：`useSyncStatus` 返回值误用（`sync?.syncing` 应为 `sync.status?.syncing`）；`parseCrew` 的 Map 泛型推断为 unknown；`(s as Record<string,number>)` 越权转换。现在 `tsc --noEmit` 干净，建议后续 build 前都跑一遍。

**验证**：tsc ✅ / vite build ✅（gzip 68.7KB js + 8.2KB css）。浏览器实测：载入真实排轴（天鼓 396 万血）→ 完整计算 → 结果与旧版一致（DPS 825,929.3、轴内击杀 ✓）；三皮肤 × 明暗矩阵截图评审通过；1024×880（桌面默认窗）粘性面板/毛玻璃顶栏正常；760×620（最小窗）与 390×844（手机）单栏无横向溢出。

**注意**：本次只重建了 `web/dist`，未重新打包 exe/zip——下次跑 `build_package.py` 时新前端自动进包。`功能说明.md` 第三节已同步新布局描述。

## 2026-09-16 21:20　补记：GitHub API 限额问题的根治

**背景**：本次维护中等了约 25 分钟等 GitHub API 匿名限额（60 次/小时/IP）重置——sync_endaxis.py 的查版本与 tarball 下载当时都只走 api.github.com，无替代通道。

**修复**（不改任何计算语义）：
- `sync_endaxis.py`：① 支持 `GITHUB_TOKEN`/`GH_TOKEN` 环境变量（认证后 5000/h）；② API 403 时自动回退 commits atom feed（`github.com/<repo>/commits/main.atom`，不占 API 限额），并打印限额重置时间；③ tarball 下载改走 `codeload.github.com` 直连（同一份内容，不占 API 限额）。
- `dps_end/updates.py`（应用内更新检查，随包发布）：同样加 token 支持与 atom feed 回退，结果带 `via: api/atom` 字段。
- 已验证：正常 API 路径、模拟 403 后两个脚本的 atom 回退、codeload 通道（gzip 头）全部通过；重打包 exe 冒烟正常；build_package.py ✅（SHA256 见下方 zip 最新值）。

**用户可选操作**：在 github.com/settings/tokens 生成一个免费 PAT（公共仓库只读访问无需勾选任何 scope），设置用户环境变量 `GITHUB_TOKEN` 后，两个脚本会自动带上认证头，限额升到 5000/h。不设置也没关系——现在的回退链路已经不依赖那个 60/h 池了。

## 2026-09-16 20:09–21:05　例行维护（计算核心更新 + 新干员 + 官方名同步）

**上游版本变化**：Endaxis `a56d77e8` → `d248cf30`（21 个提交；本地 dist 与交付包 exe 哈希一致，无未打包改动）

**做了什么**：
1. 网络探测 OK（data.akedata.wiki 可达）。GitHub API 匿名限额（60/h）耗尽导致同步推迟到 20:09 执行；等待期间用 commits atom feed + raw.githubusercontent 预检了上游变更与补丁锚点（不受 API 限额）。
2. 备份：`tools/_backup_endaxis_a56d77e8.zip`（108MB，23656 文件，全量含 node_modules，可独立还原）。
3. `sync_endaxis.py` 同步成功：上游改了 HitHandler.ts（+73/-11），补丁锚点仍完整匹配，DPS-END 敌方状态快照补丁照常应用（2 处）；自带回归 = 3,700,482，与维护前基线一致。
4. 依赖：dpsend-runtime.package.json 四包（@vue/tsconfig/lodash/vite-node/vue-i18n）已装版本全部满足，无新增外部导入，无需 npm install。
5. 新干员对比：akedata CharacterTable 31 人 ⇋ upstream 31 人，双向无缺口（上游本次新增 **typhoeus 提弗洛斯**）。
6. typhoeus 实测：按 examples/样例配置.json 构造最小 `__native` 配置（operators 嵌套、UTF-8 无 BOM），harness 直跑通过（21 hits，LMDI 归属 `typhoeus:22168+0` 正常）。新武器 umbra-of-frigid（寒夜幽影）随轴验证。
7. catalog 重建：79 武器 / 258 装备件，`非字符串名称: 0`。新增武器 sufferings-end、umbra-of-frigid，新增装备件 15 个（集成实训×6、险关×3、长息×2、潮涌系×4）。
8. 官方名刷新：official_names.json gear +15（全部解析出官方名）；weapons 20 条不变——术式具类武器不在 AKEData EquipTable→ItemTable 链路（既有限制，非本次回归），新武器中文名已由上游 zh 语料提供。
9. names.json 刷新：31 干员 / 92 敌人（typhoeus→提弗洛斯 已在映射；与上次维护结果无差异）。
10. 报告英文名自查（标准轴 + typhoeus 轴全单元格扫描）：
    - 武器/潜能 EN 名泄漏（`Umbra of Frigid Eventide`、`Samifjod Fletching`）→ LABEL_CN 追加（AKEData I18nTextTable_CN 子串检索验证）。
    - typhoeus 效果 id → LABEL_CN 追加 10 条：启示、猎矢、浮空、箭雨、箭阵×2、猎物清点、萨米制箭工艺×2、身形如风（windform）、缓速（slowed），全部经 AKEData 子串检索核验。
    - **根因修复**：无名敌方减益在快照里是运行时随机 uid（`collect.ts:720` 对无 id 效果用 `Math.random().toString(36).substring(2,9)`，跨运行必变，无法映射）。处理：HitHandler 快照补丁（2 处）增加 `stat: en.stat?.modifier` 字段，`sync_endaxis.py` 补丁串同步更新，report.py 减益标签按 `id → stat → 原样` 兜底。修复后减益列显示「缓速 40.00%；箭阵 16.00%」、DoT 跳伤名「提弗洛斯箭阵跳伤N」。
11. 打包：PyInstaller 成功；dist/DPS-END.exe、web/dist、dps_end/data/{catalog,names}.json、功能说明.md、.akedata_cache/official_names.json 已同步进交付目录；`build_package.py` ✅（2638 文件路径+CRC 逐一比对通过，zip 82.7MB）。
12. 冒烟：打包后 exe 启动正常（窗口「DPS-END · 终末地排轴伤害计算器」，Responding=True），已结束全部实例并复跑 build_package.py ✅。

**测试结果**：
- 四用例回归（同步后、快照补丁修改后各一次）与维护前基线完全一致：原样 3,700,482 / 护甲→50式应龙轻甲 2,532,224（-31.57%）/ 武器→peco-5 2,601,532（-29.70%）/ 普攻12→1 3,625,967（-2.01%）。上游未调既有数值。
- typhoeus harness 可算；catalog `非字符串名称: 0`；build_package.py 两次 ✅。

**遗留问题**：
1. `directMultiplier`：增益时间线中引擎对「无 id 无名直伤倍率状态」的默认显示名，在 typhoeus 身上可能是猎物清点的强化射击（×1.2~1.6）或应弦而倒的终结技增伤，来源多义，未敢断言映射，留人工处理（不影响伤害数值与归因）。
2. `dpsend-harness.ts` 的 `setLocale('zhCN')` 因 `normalizeLocale` 不识别缺连字符写法而实际回落到 en——这是报告标签走「英文原名→LABEL_CN 中文」模式的根源。若未来改为 `setLocale('zh-CN')`，需同步清理 LABEL_CN 里的英文原名键（Khravengger、Brigand's Calling 等），否则双语混排。
3. 术式具类武器官方名不走 AKEData 武器链路（既有限制），新武器中文名依赖上游 zh 语料。
4. 上游 `src/data/weapons/sword/5/dessert-moment.ts` 为空文件（0 行），catalog 未收录，等上游补数据。

## 2026-09-16 深夜　大版本任务（新干员验证 / 更新功能 / PULSE 归因 / 竞速配队）

**任务1 自动维护盘点**：上游已同步至 d248cf30（含提弗洛斯），catalog 79 武器/258 部件，官方名已刷，dist=包内 exe。详见上一次记录。

**任务2 提弗洛斯（提诀赛黎天鼓轴）**：修复前 3,574,168（差 4M 目标约 -10.7%）。
- 修复①（回放保真）：新版导出轴 track 带 `originiumArtsPower` / `gaugeEfficiency` / `linkCdReduction`，上游 UI 导入时由 normalizers.ts 合成 stats 面板，harness 回放漏了这步 → 已在 dpsend-harness.ts 补齐（对齐 DEFAULT_ACTOR_STATS）。此轴该字段不直接改变数值（天鼓未被浮空），但对齐上游语义。
- 修复②（数值 bug，+55,713 → **3,629,881**）：上游条件被动的「解除触发」在条件状态被消耗任意一层时就解除增益（解除 consume 无 condition）——提弗洛斯「猎物清点」×1.6 只打中每轮第一发空中射击。已在 harness 后处理统一补「条件状态还剩 ≥1 层就保留」。旧轴回归 3,700,482 无损。
- **遗留**：仍距 ≈4M 差 ~8%。蓄力箭爆裂（法术爆发 2.35M）是提弗洛斯主要输出，其公式链（术式强化系数/等级系数）与游戏的对应关系需上游或实测数据进一步校准；已定位但未能在本轮闭合。

**任务3 自动更新/同步**：新增 `dps_end/engine_sync.py` + `/api/engine/update|status` + 页脚「一键更新」按钮（下载→打补丁→冒烟→切版本→失败自动回滚）。数据同步/官方名同步沿用既有 worker（本次维护已验证可用）。

**任务4 PULSE 归因（新工作表「PULSE贡献归因」）**：
PULSE（Provision-Universal Logarithmic Synergy Evaluation，脉冲归因）＝ LMDI拐力 + 技力催化 + 充能催化（附着催化单列经手）。
- 技力催化_i = 技力产出_i × 全队每点技力伤害（总伤÷总耗）——先锋的技力供给入账；
- 充能催化_ij = 提供给 j 的能量 ÷ j 终结技耗能 × j 终结技实际伤害——洁尔佩塔类充能天赋入账；
- 附着催化：施加的附着被消耗引发的反应伤害，经手单列（提弗洛斯 1,491,784 显性化）。
提诀赛黎轴验证：PULSE 合计 3,075,736（拐力部分）全队可加 ✓；排序 诀 1.29M / 梨诺 0.97M / 赛希 0.78M，赛希的充能催化 48.6K 与提弗洛斯的附着经手 1.49M 均可见。

**任务6 三队竞速（50抗天鼓，90s 轴）**：初步构筑结果 A伊冯 25,413 / B庄方宜 4,443 / C纯物理 8,983 —— 远未达到竞速水平（需 ~44K DPS）。原因：盲写轮次无装备面板与精调轴支撑（参考轴 62K DPS 是手工精调产物）。配置已存 测试轴/A伊冯.json、B庄方宜.json、C纯物理.json 作为起点，竞速优化需在 Endaxis 内精调后回灌验证。

**验收对照**：任务1 ✓ / 任务2 部分（+5.6万修复，剩 ~8% 待上游/实测校准）/ 任务3 ✓ / 任务4 ✓ / 任务6 未达竞速（初步构筑）/ 打包 ✓。

## 2026-09-17　引擎同步 d248cf30 → 13b6905c

- sync_endaxis.py 全流程通过（备份/下载/补丁×2/回归 3,700,482 ✓）。
- 提诀赛黎轴复测：3,629,881（与 13b6905c 前完全一致——上游本轮未改动提弗洛斯反应链，~8% 差距仍待上游）。
- catalog 重建：79 武器/258 部件，武器技能 226→232（上游补了部分武器技能数据）。
- names 31 干员、官方名 258 装备/20 武器 刷新。
- 打包：exe 重建，src/ 目录整体同步进交付包（3489 文件），目录⇋zip ✅（SHA256 961f0781…）。

## 待办（已定案）：项目更名 DPS-END → **DPScope · 伤害观测台**

用户已选定新名。迁移清单（一次性完成，避免半迁移状态）：
1. `dps_end/` 包 → `dpscope/`（全模块改名+import 更新）；入口 DPS-END.py → dpscope/main
2. DPS-END.spec → DPScope.spec（name/icon 不变，hiddenimports 前缀同步）；DPS-END.bat / DPS-END网页版.bat
3. 交付目录 DPS-END-v1.0-x64 → DPScope-x64；build_package.py 的 PKG/OUT 与 zip 名；环境自检与修复.bat
4. 前端品牌位：页头 DPS-END → DPScope、副标「伤害观测台」、index.html title；版本徽章保留 v1.0
5. server Server-Version 头、updates.py 文案、report.py 表头「DPS-END · 终末地排轴伤害报告」→「DPScope · …」
6. README/功能说明 全文替换；归因表更名：PULSE贡献归因 → **贡献归因**（直伤贡献(LMDI)/技力催化/充能催化/附着催化(经手)，去 PULSE 缩写）
7. 全量回归：旧轴 3,700,482、提诀赛黎轴 3,629,881、build_package.py ✅、目录⇋zip 一致
注意：rename 后首次交付 zip/目录都叫 DPScope-x64；bat 与文档里的旧名引用全部清零（grep 验证）。

## 待办（用户已定案）：项目更名 DPS-END → **DPScope · 伤害观测台**；PULSE → **贡献归因**（去缩写）

迁移清单（必须用 Write/Edit 工具逐文件迁移，Bash 直写源码会被安全钩子拦截）：
1. `dps_end/` → `dpscope/`（copytree 后逐文件把 `dps_end.` → `dpscope.`、`from dps_end` → `from dpscope`）
2. DPS-END.py 入口（内容改名 dpscope，文件名保持 DPS-END.py 供 spec 引用，或同步改 spec）
3. DPS-END.spec：hiddenimports 前缀、name='DPScope'
4. build_package.py：PKG='DPScope-x64'、OUT='DPScope-x64.zip'；app.py/sync_endaxis.py 里的引用
5. server.py：Server-Version 头 'DPScope/1.0'；report.py 表头「DPS-END · …」→「DPScope · …」；PULSE贡献归因 → 贡献归因（去 PULSE 缩写，方法说明同步改写）
6. 前端：页头 DPS-END → DPScope、副标「伤害观测台」、index.html title；npm build
7. 交付目录 DPS-END-v1.0-x64 → DPScope-x64；环境自检与修复.bat、DPS-END.bat、DPS-END网页版.bat 改名与内容
8. README/功能说明 全文替换旧名；grep 验证 `dps_end|DPS-END|PULSE` 零残留（vendor 与历史日志除外）
9. 全量回归：旧轴 3,700,482 / 提诀赛黎轴 3,629,881 / build_package.py ✅ / 目录⇋zip 一致 / exe 冒烟

## 2026-09-17　项目更名：End-DPScope · 伤害观测台

- 交付目录/zip：End-DPScope-v1.0-x64(.zip)；exe：End-DPScope.exe（spec name 已改）
- 品牌位：窗口标题、前端页头、index.html title、报告表头、Server-Version 全部改为 End-DPScope
- 归因表去缩写：工作表「贡献归因」，标题「贡献归因 · 全链路（LMDI拐力 + 技力催化 + 充能催化）」，方法说明去 PULSE 措辞
- bat：End-DPScope.bat / End-DPScope网页版.bat / 安装环境.bat（exe 名同步）
- 内部 Python 包名仍为 dps_end（用户不可见，避免大范围 import 风险；如需可后续单独迁移）
- docs（README/使用指南/功能说明）DPS-END → End-DPScope

## 2026-09-19　更名收尾

- 交付目录与 zip：**End-DPScope-v1.0-x64**（SHA256 600dd42a…）；exe：End-DPScope.exe
- 冒烟：窗口标题「End-DPScope · 伤害观测台」、Responding=True；/api/engine/status 与 /api/catalog 正常
- 归因表最终名：「贡献归因」（全链路：直伤/LMDI拐力 + 技力催化 + 充能催化 + 附着催化经手）

## 2026-09-20　「立即同步死档」修复

根因：worker 每步受「每日/每周」调度阈值拦截，近期同步过就整轮静默跳过（点击→线程启动→什么都不刷→界面无反馈）；且所有异常被 try/pass 吞掉、错误不回传。
修复：①「立即同步」改 force=True 全量强制刷新；②每步结果/错误写入 steps 并经 /api/sync/status 回传；③同步中/失败/成功在页脚有文字反馈；④顺带修复重写 worker 时丢失的 refresh_names 导入（NameError，会让名称同步静默失败）。
验证（dev server 实测）：POST /api/sync/run → names ✓ official ✓ enemy ✓（远端无更新按 ok+note 处理）→ lastSync 正确刷新。

## 2026-09-19　安全加固 + 首次公开发布

- Mimosa 扫描：15 项 → 0 项。修复：拉取域名白名单+DNS 解析校验（akedata/engine_sync/sync_endaxis/updates/verify_crew_overrides）、缓存路径白名单+越界检查、临时文件 mkdtemp、refresh_names 丢失导入修复。
- 发布：github.com/Lex1slt/End-DPScope（main + tag v1.0 + Release 附 End-DPScope-v1.0-x64.zip）。
- 冒烟：End-DPScope.exe 启动正常，同步/更新/目录接口全部 200。
