# End-DPScope 使用指南

> 明日方舟：终末地 排轴伤害计算器 —— 导入排轴，一键生成Excel计算报告

---

## 一句话总结

把排轴JSON文件拖进来，得到一个Excel表格，里面写着**每一段伤害的计算过程**。

---

## 需要联网吗？

**日常使用不需要联网。** 所有计算都在你电脑上完成。

只有在以下两种情况下才需要联网：
1. **首次安装**（下载依赖包，约5分钟）
2. **想更新干员/敌人中文名** 时（可选，不影响计算）

---

## 三分钟上手

### 前提（只需做一次）

你需要的软件：
- **Python 3.10+**（[python.org](https://www.python.org/downloads/) 下载，安装时勾选 "Add to PATH"）
- **Node.js 18+**（[nodejs.org](https://nodejs.org/) 下载 LTS 版本）

安装步骤（打开命令提示符或 PowerShell，逐行输入）：

```
cd "D:\DPS END"
pip install -r requirements.txt
cd vendor\endaxis
npm install
cd ..\..
```

看到 "added xxx packages" 就是成功了。**以后不用再做。**

### 第一步：获得排轴JSON

去 [Endaxis 排轴终端](https://www.end-axis.com/) 编辑你的轴，然后点 **导出** 按钮，得到一个 `.json` 文件。

> 没用过 Endaxis？它的 [B站教程](https://www.bilibili.com/video/BV1gSSvB6E69/) 和 [文本教程](https://ccne3sw0x16w.feishu.cn/wiki/Df1Zw6gnniT9hnkMXUxcIYs5nAc) 可以帮你上手。
> 本程序支持它导出的 `.json` 文件，不需要额外格式转换。

### 第二步：生成Excel报告

在命令行输入一行命令：

```
python -m dps_end report 你的排轴文件.json
```

等几秒钟，会在同一目录生成 `你的排轴文件_DPS报告.xlsx`。

### 第三步：打开Excel看结果

双击打开报告，重点看第一个工作表 **「计算过程」**：

- **上面几行**：总伤害、DPS、DPSP 等总结数据
- **下面每一行**：一段伤害的计算过程
  - 靠左：时间、干员、动作（如"汤汤战技hit1"）
  - 靠前：**实际伤害（非暴击）**、**暴击伤害**（与游戏里跳的数字直接对照）
  - 再往右：敌人附着层数、debuff、失衡状态
  - 再往右：各乘区数值及**每条增益的来源**（伤害计算的全过程）

---

## 常用选项

### 换敌人

```bash
# 默认：0抗性、10亿血训练木桩
python -m dps_end report 排轴.json

# 用排轴里保存的敌人（如战争回响天鼓：396万血、50全抗）
python -m dps_end report 排轴.json --enemy timeline

# 指定游戏内敌人（按ID查AKEData数据）
python -m dps_end report 排轴.json --enemy eny_0082_hsbear

# 手动指定血量和抗性
python -m dps_end report 排轴.json --enemy-hp 5000000 --enemy-res "cryo=30,heat=0"
```

### 面板校准（让数字对上你的游戏画面）

Endaxis 不记录装备的暴击副词条，所以模拟面板的暴击是基础值（5%/50%）。
如果你的实战面板暴击更高，伤害会差很多。用游戏内面板实际值覆盖：

```bash
python -m dps_end report 排轴.json --crit-rate "last-rite=62.5,tangtang=55" --crit-dmg "last-rite=150"
```

格式是 `干员英文名=数值`，多个干员用逗号隔开。暴击率填面板上的百分数（如62.5填62.5%），暴伤同理。

### 不暴击 / 全暴击

默认报告是"期望伤害"（考虑暴击概率的平均值）。如果你想看非暴击或全暴击的数字：

```bash
python -m dps_end report 排轴.json --crit never    # 全部按不暴击算
python -m dps_end report 排轴.json --crit always   # 全部按暴击算
```

但主表「计算过程」每一行**同时给出了非暴击伤害和暴击伤害两列**，你可以直接对照游戏里的跳字——非暴击行对应不暴击的那次，暴击行对应暴击的那次。

---

## 不用Endaxis，直接写配置

如果你不想用 Endaxis 排轴器，也可以直接写一个JSON配置文件：

```json
{
  "__native": {
    "name": "我的轴",
    "durationSec": 60,
    "enemy": { "id": "eny_0082_hsbear", "level": 90, "hpMultiplier": 3.6 },
    "operators": [
      {
        "operator": "last-rite",
        "level": 90,
        "weapon": { "slug": "khravengger", "level": 90 },
        "gears": ["tide-fall-light-armor@3,3,3,3", "tide-surge-gauntlets@3,3,3,3"],
        "skills": { "basicAttack": 12, "battleSkill": 12, "comboSkill": 12, "ultimate": 12 },
        "rotation": [
          { "t": 5.0, "skill": "basicAttack", "segment": 1 },
          { "t": 5.7, "skill": "basicAttack", "segment": 2 },
          { "t": 17.5, "skill": "battleSkill" },
          { "t": 21.8, "skill": "comboSkill" },
          { "t": 52.5, "skill": "ultimate" }
        ]
      }
    ]
  }
}
```

保存为 `.json` 文件后，用法完全一样：`python -m dps_end report 配置.json`

- `rotation[].t` 是动作开始时间（秒）
- `skill` 可选值：`basicAttack`（普攻）/ `battleSkill`（战技）/ `comboSkill`（连携）/ `ultimate`（终结技）/ `finisher`（处决）/ `dive`（下落）
- 普攻加 `"segment": 1~4` 指定第几段
- 干员英文名参考 `vendor/endaxis/src/data/operators/` 目录下的文件名

完整样例见 `examples/样例配置.json`。

---

## 其他功能

```bash
# 预览排轴结构（不生成Excel，只看解析结果）
python -m dps_end dump 排轴.json

# 更新干员/敌人中文名（需要联网，不影响计算）
python -m dps_end refresh-names

# 查看版本
python -m dps_end --version
```

---

## 数据从哪来？

| 数据 | 来源 | 是否联网 |
|---|---|---|
| 伤害乘区公式 | Endaxis 开源模拟器（已内置） | 否 |
| 干员技能倍率/天赋/潜能 | Endaxis 开源数据（已内置） | 否 |
| 装备词条/套装效果 | Endaxis 开源数据（已内置） | 否 |
| 敌人属性模板 | AKEData 解包数据（已缓存到 `.akedata_cache/`） | 否 |
| 干员/敌人中文名 | `dps_end/data/names.json`（已内置） | 否 |
| 更新名称映射 | `python -m dps_end refresh-names` | **是**（可选） |

简单说：**装好之后断网也能用。**

---

## 常见问题

**Q: 伤害和游戏里不一样？**

A: 模拟计算的是期望伤害（考虑暴击概率的平均值）。游戏里每次跳字要么是非暴击、要么是暴击——主表「计算过程」里每行都同时给出了这两个数字，你可以逐hit对照。

如果所有hit都比游戏低很多，检查是否需要面板校准（`--crit-rate`）。如果个别hit对不上，可能是增益时序的微差，属于模拟器的固有限制。

**Q: 新干员能用吗？**

A: 取决于 Endaxis 是否已经制作了该干员的数据。运行 `python sync_endaxis.py --check` 检查上游是否有更新。

**Q: 换电脑怎么迁移？**

A: 把整个 `DPS END` 文件夹复制过去。新电脑需要安装 Python 和 Node.js，然后在 `vendor/endaxis` 下执行 `npm install`。

**Q: DPSP是什么？**

A: Damage Per Skill Point——总伤害除以总技力消耗。衡量每点技力能打出多少伤害，用于比较不同轴的技力利用效率。

---

## 文件结构速查

```
DPS END\
├── dps_end\              ← 本程序（Python代码）
├── vendor\endaxis\       ← Endaxis模拟器（Node.js代码，含依赖）
├── output\               ← 生成的Excel报告
├── examples\             ← 原生配置样例
├── .akedata_cache\       ← AKEData数据缓存（约40MB）
├── sync_endaxis.py       ← 同步Endaxis更新的脚本
├── requirements.txt      ← Python依赖
└── 使用指南.md            ← 本文件
```
