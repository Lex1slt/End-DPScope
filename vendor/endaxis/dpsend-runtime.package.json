{
  "name": "dpsend-runtime",
  "private": true,
  "description": "DPS-END 计算所需的最小依赖集。计算 harness 只跑 TS 逻辑、不渲染 Vue 组件，因此不需要上游 Endaxis 的完整开发依赖（echarts / element-plus / typescript / prettier，约 299MB）。只装本清单约为 45MB。",
  "dependencies": {
    "@vue/tsconfig": "^0.7.0",
    "lodash": "^4.18.1",
    "vite-node": "^3.0.0",
    "vue-i18n": "^11.4.10"
  }
}
